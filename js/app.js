
window.sr = new scrollReveal( config );

var config = {
        mobile: true,
        reset:  false,
        delay:  'once',
        vFactor: 0.10
      };

$( document ).ready(function() {
    $('body').fadeIn('slow');
});